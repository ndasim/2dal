version: "3"
services:
  app:
    container_name: 2dal
    restart: always
    build:
      dockerfile: Dockerfile
      context: .
    environment:
      - VIRTUAL_HOST=2d.al
      - LETSENCRYPT_HOST=2d.al
